export const INFER_MODE = {
    QUERY: "query",
    RAW: "raw",
} as const;

export const ORDER = {
    ASC: "asc",
    DESC: "desc",
} as const;

export const NULLS = {
    FIRST: "first",
    LAST: "last",
} as const;
